let name = "lucas";
 
let age = 30;

let city = "São Paulo";

if (age >= 18) {
    console.log(`${name} é maior de idade e mora em ${city}.`);
}

// Este código define três variáveis: name, age e city. Em seguida, verifica se a idade é maior ou igual a 18 anos. Se for, ele imprime uma mensagem no console indicando que a pessoa é maior de idade e onde ela mora.
// Você pode modificar os valores das variáveis para testar diferentes cenários.
// obrigado :)